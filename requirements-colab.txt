# Colab-optimized dependencies for VERL + LangGraph Multi-Agent Coding Framework
# This file is optimized for Google Colab environments

# Core framework dependencies
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
click>=8.0.0
rich>=13.0.0
typer>=0.9.0

# LangGraph and LangChain - Colab compatible versions
langgraph>=0.0.60
langchain>=0.1.0
langchain-core>=0.1.0
langchain-openai>=0.0.8
langchain-anthropic>=0.1.0
langchain-huggingface>=0.0.3

# LLM providers
openai>=1.0.0
anthropic>=0.18.0

# HuggingFace for local models
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.20.0
bitsandbytes>=0.41.0  # For quantization
sentencepiece>=0.1.99  # For tokenization
protobuf>=3.20.0

# Code execution and analysis (Colab-friendly versions)
# Note: Docker not available in Colab, so we exclude it
flake8>=6.0.0
black>=23.0.0
isort>=5.12.0
bandit>=1.7.5

# Async and concurrency
aiofiles>=23.0.0
httpx>=0.24.0

# Data processing
pandas>=1.5.0  # Colab has pandas pre-installed
numpy>=1.21.0  # Colab compatible version
jsonlines>=3.1.0

# Monitoring and logging
structlog>=23.0.0

# Development and testing (lighter versions for Colab)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Colab-specific optimizations
ipywidgets>=8.0.0  # For interactive widgets
tqdm>=4.65.0  # Progress bars
matplotlib>=3.5.0  # Visualization
seaborn>=0.11.0  # Enhanced plotting

# Optional: Weights & Biases for experiment tracking
# wandb>=0.15.0

# Memory optimization for Colab
psutil>=5.9.0
GPUtil>=1.4.0